# API Base URL
# Leave unset to use the Vite proxy (/api -> localhost:8080)
# Or set explicitly:
#   /api                     - Vite proxy (default, avoids CORS)
#   http://localhost:3000    - Mock server
#   https://api.grengin.com  - Hosted version
# VITE_API_BASE=/api
